Sources for the plugin `plugin.appodeal.StartApp`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.StartApp" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
